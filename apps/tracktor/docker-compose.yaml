services:
  tracktor-app:
    image: ghcr.io/javedh-dev/tracktor:latest
    container_name: tracktor
    restart: always
    ports:
      - "3333:3000"
    volumes:
      - ./data:/data
    environment:
      - NODE_ENV=production
      - PUBLIC_DEMO_MODE=false

# volumes:
#   tracktor-data: null